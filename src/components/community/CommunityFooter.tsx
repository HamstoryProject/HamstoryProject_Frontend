import styled from "styled-components";

const Wrapper = styled.div`
    width: 100%;
    height: 100px;
    display: grid;
`;

export default function CommunityFooter() {
    return(
        <Wrapper>
            FOOTER
        </Wrapper>
    );
}